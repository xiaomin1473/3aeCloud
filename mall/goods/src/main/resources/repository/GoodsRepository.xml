<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store.ae.goods.repository.GoodsRepository">
    <select id="queryAll" resultType="Goods">
        select goods_id, business_id, brand_id, category_type, goods_name, amount, unit, price, volume, img_url, gmt_create, gmt_modified
        from tb_goods
    </select>
</mapper>