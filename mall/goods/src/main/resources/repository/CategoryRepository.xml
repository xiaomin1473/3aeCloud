<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store.ae.goods.repository.CategoryRepository">
    <select id="queryAll" resultType="Category">
        select category_name, category_type, grade_type
        from tb_goods_category
    </select>
</mapper>